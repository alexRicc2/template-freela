{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "DATABASE_URL",
    "DATABASE_SSL",
    "PAYLOAD_SECRET",
    "PAYLOAD_URL",
    "FRONTEND_URL",
    "PAYLOAD_API_URL",
    "PUBLIC_PAYLOAD_URL",
    "VERCEL"
  ],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**", ".vercel/output/**"]
    }
  }
}
